import{_ as m,r as i,o as c,c as l,b as e,d as o,e as n,a as d}from"./app-C95d1NCF.js";const s={},a=e("h1",{id:"moment-timezone",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#moment-timezone"},[e("span",null,"Moment Timezone")])],-1),p=e("h2",{id:"搞懂-moment-timezone",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#搞懂-moment-timezone"},[e("span",null,"搞懂 moment-timezone")])],-1),r={href:"https://link.zhihu.com/?target=https%3A//momentjs.com/timezone/",target:"_blank",rel:"noopener noreferrer"},z=d('<p>使用时需要先安装  <code>yarn add moment-timezone</code>。</p><p>这里理一理两个 API：<code>moment(time).tz(timezone)</code>  和  <code>moment.tz(time, timezone)</code>  的区别。</p><ol><li>时区</li></ol><p>首先，不管哪个 &quot;时间&quot;，在地球上都是同一个 &quot;时间&quot;，只不过在不同时区的显示不一样。</p><p>虽然这是个常识，但在实际操作中很容易混乱，所以得再理解一下。</p><p>北京是白天，纽约是晚上，但打通视频，&quot;此刻&quot; 是同一个时刻，地球上的同一个时刻。</p><p>只不过 &quot;显示&quot; 的时间不一样。</p><ol start="2"><li>moment(time).tz(timezone)</li></ol><p><code>moment(time).tz(timezone)</code>  是把一个 &quot;<strong>时间</strong>&quot;（一般是自己当前时区的时间）的时区，变为指定的时区。</p><p>主要是用于显示，所以插件官网上的例子，都是跟着  <code>.format()</code>。</p><p>请注意这个 &quot;<strong>时间</strong>&quot;，就是不管跟着的  <code>.tz()</code>  怎么变，其实都是同一个时间。</p><p>所以用  <code>.unix()</code>  取值时，都是同一个值，因为它就是同一个时间。</p><p>比如获取到北京的某个时间，那就是看北京的这个时间，在全世界其它地方显示是几点。</p><ol start="3"><li><p>moment.tz(time, timezone)</p><blockquote><p><code>moment.tz(time, timezone)</code>  是创建一个 moment 对象，这里分 2 种情况:</p><ol><li>如果传入的  <code>time</code>  上携带了时区，那就跟  <code>moment(time).tz(timezone)</code>  没任何区别，都是把某个时间的时区，变为指定的时区。</li><li>如果传入的  <code>time</code>  上没携带时区，那就不一样了。是创建一个指定  <code>timezone</code>  的时间。比如在北京操作，传入  <code>(2022-05-22 08:00:00, 纽约)</code>，那就是创建一个纽约早上 8 点的时间（而不是北京早上 8 点的时间，在纽约显示是几点）。</li></ol></blockquote></li><li><p>获取时间在指定时区的 unix</p></li></ol><p>经常遇到这种需求：获取一个时间点在其它时区的  <code>unix</code>。<br> 很容易这么干： <code>moment(time).tz(timezone).unix()</code>  或 <code>moment.tz(time, timezone).unix()</code> (若  <code>time</code>  为 moment 对象，或为时间字符串但携带了时区信息) 然后就会发现，无论  <code>timezone</code>  怎么变，得到的  <code>unix</code>  值都一样。 这是因为上面都是只更改时间的时区，但这个 &quot;时间&quot; 本身一直没变。 想要创建一个指定时区的时间，得这么干： <code>const clean = moment(time).format(&#39;YYYY-MM-DD HH:mm:ss&#39;); // 清除 timezone 信息 const local = moment.tz(clean, timezone);</code></p><p>此时取值  <code>local.unix()</code>，才可以得到这个时间在指定时区的  <code>unix</code>  值。</p><ol start="5"><li>有没有更简单的办法?</li></ol><p>当然是有的。</p><p>必须吐槽一下：不管是 moment，还是 dayjs，timezone 的文档都太简略了，根本没标明其它参数。</p><p>翻看 moment-timezone 的源码会发现，<code>.tz()</code>  其实还有有第二个参数：<code>keepLocalTime</code>。</p>',20),u={href:"https://link.zhihu.com/?target=https%3A//github.com/moment/moment-timezone/blob/develop/moment-timezone.js",target:"_blank",rel:"noopener noreferrer"},h=e("p",null,"所以上面问题的解决方案，其实很简单：",-1),_=e("p",null,[e("code",null,"const local = moment(time).tz(timezone, true);")],-1),f=e("p",null,[o("多传个  "),e("code",null,"true"),o("  就行了！")],-1),b=e("p",null,"区别（若在北京操作）：",-1),x=e("p",null,'`moment(早上八点).tz(纽约); // 把 "北京早上八点" 的时区变为纽约',-1),q=e("p",null,'moment(早上八点).tz(纽约, true); // 返回一个 "纽约早上八点" 的时间`',-1);function g(k,v){const t=i("ExternalLinkIcon");return c(),l("div",null,[a,p,e("p",null,[o("moment-timezone 插件："),e("a",r,[o("https://momentjs.com/timezone/"),n(t)])]),z,e("p",null,[e("a",u,[o("https://github.com/moment/moment-timezone/blob/develop/moment-timezone.js"),n(t)])]),h,_,f,b,x,q])}const N=m(s,[["render",g],["__file","moment-timezone.html.vue"]]),T=JSON.parse('{"path":"/front-end/web/moment-timezone.html","title":"Moment Timezone","lang":"zh-CN","frontmatter":{},"headers":[{"level":2,"title":"搞懂 moment-timezone","slug":"搞懂-moment-timezone","link":"#搞懂-moment-timezone","children":[]}],"git":{"updatedTime":1718611692000,"contributors":[{"name":"Moui","email":"alanjingzhixin@gmail.com","commits":1}]},"filePathRelative":"front-end/web/moment-timezone.md"}');export{N as comp,T as data};
